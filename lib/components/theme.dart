import 'package:flutter/material.dart';

ThemeData lightMode =
    ThemeData(brightness: Brightness.light, useMaterial3: true);

ThemeData darkMode = ThemeData(brightness: Brightness.dark, useMaterial3: true);
